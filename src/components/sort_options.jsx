import React from 'react';

const SortOptions = ({ setParentState }) => {
    return (
        <div className="sort-options">
            <div>
                sort by:
                <input type="radio"/>
            </div>
        </div>
    );
};

export default SortOptions;